public with sharing class cc_extn_HomePage {
	public cc_extn_HomePage(cc_ctrl_HomePage controller) {
		String pageState = 'homeView';
		//cc_hlpr_PageStateMachine.setPageStateCookie(pageState, ApexPages.currentPage());
	}
}